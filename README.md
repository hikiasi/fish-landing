# 🐟 Океаника: Платформа для продажи рыбы

![Hero](https://i.imgur.com/your-hero-image.png) <!-- Replace with an actual screenshot -->

Это полноценный full-stack проект, разработанный на Next.js, для компании по продаже свежемороженой рыбы и морепродуктов. Платформа включает в себя витрину для розничных покупателей, отдельный раздел для оптовых клиентов (B2B), а также защищенную админ-панель для управления товарами и заказами.

## ✨ Ключевые особенности

-   **Два сценария использования**:
    -   **Розничная торговля (Retail):** Каталог товаров, корзина, быстрое оформление заказа.
    -   **Оптовая торговля (B2B):** Отдельный каталог с оптовыми ценами, формы для запроса прайс-листа и образцов.
-   **Адаптивный дизайн**: Корректное отображение на всех устройствах, от мобильных телефонов до настольных компьютеров.
-   **Интерактивные элементы**: Плавные анимации на `framer-motion` для улучшения пользовательского опыта.
-   **Админ-панель**: Защищенный раздел для полного управления контентом:
    -   Добавление, редактирование и удаление товаров для розницы и опта.
    -   Просмотр и управление входящими заявками и заказами.
    -   Загрузка изображений товаров через облачное хранилище **Vercel Blob**.
-   **SEO-оптимизация**: Структура проекта на Next.js App Router способствует лучшей индексации поисковыми системами.

## 🚀 Технологический стек

-   **Фреймворк**: [Next.js](https://nextjs.org/) (App Router)
-   **Язык**: [TypeScript](https://www.typescriptlang.org/)
-   **Стилизация**: [Tailwind CSS](https://tailwindcss.com/)
-   **UI-компоненты**: [shadcn/ui](https://ui.shadcn.com/)
-   **Анимации**: [Framer Motion](https://www.framer.com/motion/)
-   **Формы**: [React Hook Form](https://react-hook-form.com/) & [Zod](https://zod.dev/) для валидации
-   **База данных**: [Prisma](https://www.prisma.io/) ORM
-   **Хостинг БД**: [Vercel Postgres](https://vercel.com/storage/postgres)
-   **Хранение файлов**: [Vercel Blob](https://vercel.com/storage/blob)

## 📁 Структура проекта

```
/
├── prisma/                 # Схема и миграции Prisma, файл сидинга
│   ├── schema.prisma       # Модели данных
│   └── seed.ts             # Скрипт для заполнения БД начальными данными
├── public/                 # Статические ассеты
├── src/
│   ├── app/                # Основная директория Next.js App Router
│   │   ├── admin/          # Админ-панель (управление контентом)
│   │   ├── api/            # API-маршруты (продукты, заказы, загрузка)
│   │   └── page.tsx        # Главная страница
│   ├── components/         # React-компоненты
│   │   ├── layout/         # Глобальные компоненты (Header, Footer)
│   │   ├── sections/       # Крупные секции страниц (Hero, каталоги)
│   │   └── ui/             # Базовые UI-компоненты (кнопки, инпуты)
│   ├── context/            # React Context (управление состоянием корзины)
│   └── lib/                # Вспомогательные утилиты (Prisma client, cn)
├── .env.local.example      # Пример файла с переменными окружения
└── package.json            # Зависимости и скрипты
```

## ⚙️ Локальная разработка

Для запуска проекта на вашем компьютере выполните следующие шаги.

### Предварительные требования

-   [Node.js](https://nodejs.org/en/) (версия 18.x или выше)
-   [npm](https://www.npmjs.com/) или [yarn](https://yarnpkg.com/)
-   Доступ к PostgreSQL базе данных (можно использовать локальную или облачную)

### 1. Клонирование репозитория

```bash
git clone <URL_вашего_репозитория>
cd <название_папки>
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Настройка переменных окружения

Создайте файл `.env.local` в корне проекта, скопировав содержимое из `.env.local.example`.

**.env.local.example:**
```env
# База данных (подставьте URL вашей PostgreSQL базы)
# Пример: postgresql://user:password@host:port/database
DATABASE_URL="YOUR_POSTGRESQL_URL"

# Vercel Blob Storage (токен для загрузки файлов)
BLOB_READ_WRITE_TOKEN="YOUR_BLOB_READ_WRITE_TOKEN"

# Логин и пароль для админ-панели
ADMIN_USERNAME="admin"
ADMIN_PASSWORD="your_secure_password"
```
Заполните этот файл вашими реальными данными.

### 4. Настройка и заполнение базы данных

Выполните следующие команды для синхронизации схемы Prisma с вашей БД и заполнения ее начальными данными:

```bash
# Применяет схему из prisma/schema.prisma к базе данных
npx prisma db push

# Запускает скрипт prisma/seed.ts для добавления тестовых товаров
npx tsx prisma/seed.ts
```

### 5. Запуск проекта

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в вашем браузере, чтобы увидеть результат. Админ-панель будет доступна по адресу `/admin`.

## 🚀 Развертывание на Vercel

Проект настроен для легкого развертывания на платформе Vercel.

1.  **Загрузите код в Git-репозиторий** (GitHub, GitLab, Bitbucket).
2.  **Создайте новый проект на Vercel**, импортировав этот репозиторий.
3.  **Подключите Vercel Postgres**: на шаге "Configure Project" найдите раздел "Storage" и добавьте базу данных Postgres. Vercel автоматически создаст и подставит переменную `DATABASE_URL`.
4.  **Подключите Vercel Blob**: на том же шаге "Configure Project" подключите Blob Storage. Vercel автоматически подставит `BLOB_READ_WRITE_TOKEN`.
5.  **Добавьте учетные данные админа**: в настройках проекта ("Environment Variables") добавьте `ADMIN_USERNAME` и `ADMIN_PASSWORD`.
6.  Нажмите **"Deploy"**.

Vercel автоматически выполнит все шаги из `build` скрипта (`prisma generate`, `prisma db push`, `tsx prisma/seed.ts`, `next build`), и ваш сайт будет готов к работе.

---